{

  "$id": "/chain_matic/volume_ticks_hourly.json",
  "title": "volume_ticks_hourly",
  "description": "Aggregated hourly Volume Data of tokens on different platforms",
  "properties": {
    "bucket": {
      "type": "string",
      "format": "date-time",
      "description": "Database type: timestamp with time zone. Default value: null"
    },
    "token_id": {
      "type": "number",
      "description": "Database type: integer. Default value: null"
    },
    "platform_id": {
      "type": "number",
      "description": "Database type: integer. Default value: null"
    },
    "volume": {
      "type": "string",
      "description": "Database type: text. Default value: null"
    }
  },
  "required": [
    "bucket"
  ],
  "type": "object"
}
